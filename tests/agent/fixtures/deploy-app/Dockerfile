# Minimal placeholder so the deploy-config scenario sees a deployable app:
# a single Dockerfile at the repo root, listening on :80 with a /up health check.
# This fixture exercises deploy CONFIG generation, not an actual build/run.
FROM alpine:3.20
EXPOSE 80
CMD ["true"]
