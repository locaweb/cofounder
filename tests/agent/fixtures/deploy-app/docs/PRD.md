# PRD — Tarefas (already built)

A small task-list app. It is **already implemented and working** (Dockerfile at
the repo root, listens on port 80, health check at `GET /up`, uses a PostgreSQL
accessory). For this exercise we only need to set up its **preview deployment
configuration** — not build or change the app.
